# airline-gui

Frontend for the Airline Reservation System, built with React and Vite.

## Scripts

- `npm start` starts the Vite development server on `http://localhost:3000`.
- `npm run build` creates a production build in `dist/`.
- `npm test` runs Vitest in non-watch mode.
