name: Quality Zero Platform

permissions:
  contents: read
  id-token: write
  pull-requests: write

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  shared-scanner-matrix:
    permissions:
      contents: read
      id-token: write
      pull-requests: write
    uses: Prekzursil/quality-zero-platform/.github/workflows/reusable-scanner-matrix.yml@610a01243134b5a222060b376b4b915fae61c48c
    with:
      repo_slug: ${{ github.repository }}
      event_name: ${{ github.event_name }}
      sha: ${{ github.event.pull_request.head.sha || github.sha }}
      branch_name: ${{ github.head_ref || github.ref_name }}
      pull_request_number: ${{ github.event.pull_request.number || '' }}
      pull_request_author: ${{ github.event.pull_request.user.login || '' }}
      pull_request_head_ref: ${{ github.event.pull_request.head.ref || github.head_ref || '' }}
      platform_repository: Prekzursil/quality-zero-platform
      platform_ref: 610a01243134b5a222060b376b4b915fae61c48c
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      CODACY_API_TOKEN: ${{ secrets.CODACY_API_TOKEN }}
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      DEEPSCAN_API_TOKEN: ${{ secrets.DEEPSCAN_API_TOKEN }}
